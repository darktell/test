Supporting operands between tags - (+,-,*,(,),^,/…..) - done 
Support adding natural numbers (1,2,3) - done
Supporting writing between tags - done 
Deleting a TAG with backspace - done, but have bug
Autocomplete suggestions - Support autocomplete also after an operand is written - done, after operand we can write and autocompleate will works, (searching by name)
MANDATORY - Adding attached dropdown to each tag. - Add the dropdown attached to the tag on the right, allowing to interact with the dropdown on each tag. - done 50/50 cuz I made like in "New variable" functionality, so just add select with a mocked value for it
Calculating values is a bonus (Assign some dummy variable values and accept the operations). - not done at all ( 
